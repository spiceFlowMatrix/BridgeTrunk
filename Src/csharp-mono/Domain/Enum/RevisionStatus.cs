namespace Domain.Enum
{
    public enum RevisionStatus
    {
        revision_start = 1,
        in_development = 2,
        in_review = 3,
        content_prep_running = 4,
        content_prep_released = 5
    }
}